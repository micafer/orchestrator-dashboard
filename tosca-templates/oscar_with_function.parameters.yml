inputs:
    wn_num:
      tab: HW Data

    fe_cpus:
      tab: HW Data

    fe_mem:
      tab: HW Data

    wn_cpus:
      tab: HW Data

    wn_mem:
      tab: HW Data
    
    storage_size:
      tab: HW Data

    admin_token:
      tab: OSCAR parameters

    oscar_password:
      tab: OSCAR parameters

    minio_password:
      tab: OSCAR parameters

    cert_user_email:
      tab: OSCAR parameters

    function_name:
      tab: OSCAR function

    function_memory:
      tab: OSCAR function
      pattern: ^([+-]?[0-9.]+)([eEinumkKMGTP]*[-+]?[0-9]*)$
      tag_type: text

    function_cpu:
      tab: OSCAR function

    function_image:
      tab: OSCAR function

    function_script:
      tab: OSCAR function
      tag_type: textarea

    function_input:
      tab: OSCAR function

    function_output:
      tab: OSCAR function

tabs:
  - HW Data
  - OSCAR parameters
  - OSCAR function